import {createProject} from "@/app/actions/projects";import {ASPECTS,TEMPLATES} from "@/lib/constants";import {Button,Card,Logo} from "@/components/ui";import Link from "next/link";import {ArrowLeft,Upload,Mic,FileText} from "lucide-react";export const metadata={title:"New project"};export default function Page(){return <main id="main" className="mx-auto min-h-screen max-w-5xl px-5 py-8"><div className="flex items-center justify-between"><Link href="/dashboard" aria-label="Back to dashboard" className="flex min-h-11 items-center gap-2 text-muted-foreground"><ArrowLeft/>Back</Link><Logo/><span className="text-muted-foreground">Step 1 of 1</span></div><form action={createProject} className="mt-12"><div className="max-w-2xl"><p className="font-mono text-primary">NEW PROJECT</p><h1 className="mt-2 text-4xl font-bold tracking-tight">Set the stage for your lyrics.</h1><p className="mt-3 text-lg text-muted-foreground">Choose a canvas and starter treatment. You&apos;ll add private media inside the editor.</p></div><label className="mt-9 block max-w-xl font-semibold">Project name<input name="title" defaultValue="Untitled lyric video" maxLength={80} required className="mt-2 min-h-12 w-full rounded-lg border bg-card px-4"/></label><fieldset className="mt-9"><legend className="text-xl font-semibold">Choose format</legend><div className="mt-4 grid gap-4 sm:grid-cols-3">{ASPECTS.map((a,i)=><label key={a.id} className="cursor-pointer"><input type="radio" name="aspect" value={a.id} defaultChecked={i===0} className="peer sr-only"/><Card className="flex min-h-40 flex-col items-center justify-center p-5 peer-checked:border-primary peer-checked:bg-secondary"><div className={a.ratio==="9:16"?"h-16 w-9 border-2 border-primary":a.ratio==="1:1"?"size-14 border-2 border-muted-foreground":"h-14 w-11 border-2 border-muted-foreground"}/><strong className="mt-4">{a.label}</strong><span className="text-muted-foreground">{a.ratio}</span></Card></label>)}</div></fieldset><fieldset className="mt-9"><legend className="text-xl font-semibold">Starter style</legend><div className="mt-4 grid gap-3 sm:grid-cols-3">{TEMPLATES.slice(0,3).map((t,i)=><label key={t.id} className="cursor-pointer"><input type="radio" name="template" value={t.id} defaultChecked={i===0} className="peer sr-only"/><Card className="p-4 peer-checked:border-primary"><strong>{t.title}</strong><p className="text-muted-foreground">{t.category} · {t.animation}</p></Card></label>)}</div></fieldset><Card className="mt-9 p-5"><h2 className="font-semibold">Next, inside the editor</h2><div className="mt-3 flex flex-wrap gap-5 text-muted-foreground"><span className="flex gap-2"><Upload/>Upload audio & video</span><span className="flex gap-2"><FileText/>Enter timed lyrics</span><span className="flex gap-2"><Mic/>AI transcription coming later</span></div></Card><div className="mt-8 flex justify-end"><Button type="submit">Create and open editor</Button></div></form></main>}
